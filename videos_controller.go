// Package shutterstockapiexplorer
// This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
package shutterstockapiexplorer

import (
    "context"
    "github.com/apimatic/go-core-runtime/https"
    "github.com/apimatic/go-core-runtime/utilities"
    "net/http"
    "shutterstockapiexplorer/models"
    "time"
)

// VideosController represents a controller struct.
type VideosController struct {
    baseController
}

// NewVideosController creates a new instance of VideosController.
// It takes a baseController as a parameter and returns a pointer to the VideosController.
func NewVideosController(baseController baseController) *VideosController {
    videosController := VideosController{baseController: baseController}
    return &videosController
}

// GetVideoList takes context, id, view, searchId as parameters and
// returns an models.ApiResponse with models.VideoDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists information about one or more videos, including the aspect ratio and URLs to previews.
func (v *VideosController) GetVideoList(
    ctx context.Context,
    id []string,
    view *models.View1Enum,
    searchId *string) (
    models.ApiResponse[models.VideoDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos")
    
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    req.QueryParam("id", id)
    if view != nil {
        req.QueryParam("view", *view)
    }
    if searchId != nil {
        req.QueryParam("search_id", *searchId)
    }
    var result models.VideoDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.VideoDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// ListVideoCategories takes context, language as parameters and
// returns an models.ApiResponse with models.CategoryDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists the categories (Shutterstock-assigned genres) that videos can belong to.
func (v *VideosController) ListVideoCategories(
    ctx context.Context,
    language *models.LanguageEnum) (
    models.ApiResponse[models.CategoryDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/categories")
    
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    if language != nil {
        req.QueryParam("language", *language)
    }
    var result models.CategoryDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.CategoryDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// GetVideoCollectionList takes context, page, perPage, embed as parameters and
// returns an models.ApiResponse with models.CollectionDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists your collections of videos and their basic attributes.
func (v *VideosController) GetVideoCollectionList(
    ctx context.Context,
    page *int,
    perPage *int,
    embed []models.EmbedEnum) (
    models.ApiResponse[models.CollectionDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/collections")
    
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    if page != nil {
        req.QueryParam("page", *page)
    }
    if perPage != nil {
        req.QueryParam("per_page", *perPage)
    }
    if embed != nil {
        req.QueryParam("embed", embed)
    }
    var result models.CollectionDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.CollectionDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// CreateVideoCollection takes context, body as parameters and
// returns an models.ApiResponse with models.CollectionCreateResponse data and
// an error if there was an issue with the request or response.
// This endpoint creates one or more collections (clipboxes). To add videos to collections, use `POST /v2/videos/collections/{id}/items`.
func (v *VideosController) CreateVideoCollection(
    ctx context.Context,
    body models.CollectionCreateRequest) (
    models.ApiResponse[models.CollectionCreateResponse],
    error) {
    req := v.prepareRequest(ctx, "POST", "/v2/videos/collections")
    
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    req.Header("Content-Type", "application/json")
    req.Json(&body)
    var result models.CollectionCreateResponse
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.CollectionCreateResponse](decoder)
    return models.NewApiResponse(result, resp), err
}

// GetFeaturedVideoCollectionList takes context, embed as parameters and
// returns an models.ApiResponse with models.FeaturedCollectionDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists featured video collections and a name and cover video for each collection.
func (v *VideosController) GetFeaturedVideoCollectionList(
    ctx context.Context,
    embed *models.Embed3Enum) (
    models.ApiResponse[models.FeaturedCollectionDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/collections/featured")
    
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    if embed != nil {
        req.QueryParam("embed", *embed)
    }
    var result models.FeaturedCollectionDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.FeaturedCollectionDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// GetFeaturedVideoCollection takes context, id, embed as parameters and
// returns an models.ApiResponse with models.FeaturedCollection data and
// an error if there was an issue with the request or response.
// This endpoint gets more detailed information about a featured video collection, including its cover video and timestamps for its creation and most recent update. To get the videos, use `GET /v2/videos/collections/featured/{id}/items`.
func (v *VideosController) GetFeaturedVideoCollection(
    ctx context.Context,
    id string,
    embed *models.Embed3Enum) (
    models.ApiResponse[models.FeaturedCollection],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/collections/featured/%v")
    req.AppendTemplateParams(id)
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Featured collection not found"},
    })
    if embed != nil {
        req.QueryParam("embed", *embed)
    }
    
    var result models.FeaturedCollection
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.FeaturedCollection](decoder)
    return models.NewApiResponse(result, resp), err
}

// GetFeaturedVideoCollectionItems takes context, id, page, perPage as parameters and
// returns an models.ApiResponse with models.VideoCollectionItemDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists the IDs of videos in a featured collection and the date that each was added.
func (v *VideosController) GetFeaturedVideoCollectionItems(
    ctx context.Context,
    id string,
    page *int,
    perPage *int) (
    models.ApiResponse[models.VideoCollectionItemDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/collections/featured/%v/items")
    req.AppendTemplateParams(id)
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Featured collection not found"},
    })
    if page != nil {
        req.QueryParam("page", *page)
    }
    if perPage != nil {
        req.QueryParam("per_page", *perPage)
    }
    
    var result models.VideoCollectionItemDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.VideoCollectionItemDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// DeleteVideoCollection takes context, id as parameters and
// returns an *Response and
// an error if there was an issue with the request or response.
// This endpoint deletes a collection.
func (v *VideosController) DeleteVideoCollection(
    ctx context.Context,
    id string) (
    *http.Response,
    error) {
    req := v.prepareRequest(ctx, "DELETE", "/v2/videos/collections/%v")
    req.AppendTemplateParams(id)
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Collection not found"},
    })
    
    httpCtx, err := req.Call()
    if err != nil {
        return httpCtx.Response, err
    }
    return httpCtx.Response, err
}

// GetVideoCollection takes context, id, embed, shareCode as parameters and
// returns an models.ApiResponse with models.Collection data and
// an error if there was an issue with the request or response.
// This endpoint gets more detailed information about a collection, including the timestamp for its creation and the number of videos in it. To get the videos in collections, use GET /v2/videos/collections/{id}/items.
func (v *VideosController) GetVideoCollection(
    ctx context.Context,
    id string,
    embed []models.EmbedEnum,
    shareCode *string) (
    models.ApiResponse[models.Collection],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/collections/%v")
    req.AppendTemplateParams(id)
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Collection not found"},
    })
    if embed != nil {
        req.QueryParam("embed", embed)
    }
    if shareCode != nil {
        req.QueryParam("share_code", *shareCode)
    }
    
    var result models.Collection
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.Collection](decoder)
    return models.NewApiResponse(result, resp), err
}

// RenameVideoCollection takes context, id, body as parameters and
// returns an *Response and
// an error if there was an issue with the request or response.
// This endpoint sets a new name for a collection.
func (v *VideosController) RenameVideoCollection(
    ctx context.Context,
    id string,
    body models.CollectionUpdateRequest) (
    *http.Response,
    error) {
    req := v.prepareRequest(ctx, "POST", "/v2/videos/collections/%v")
    req.AppendTemplateParams(id)
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Collection not found"},
    })
    req.Header("Content-Type", "application/json")
    req.Json(&body)
    
    httpCtx, err := req.Call()
    if err != nil {
        return httpCtx.Response, err
    }
    return httpCtx.Response, err
}

// DeleteVideoCollectionItems takes context, id, itemId as parameters and
// returns an *Response and
// an error if there was an issue with the request or response.
// This endpoint removes one or more videos from a collection.
func (v *VideosController) DeleteVideoCollectionItems(
    ctx context.Context,
    id string,
    itemId []string) (
    *http.Response,
    error) {
    req := v.prepareRequest(ctx, "DELETE", "/v2/videos/collections/%v/items")
    req.AppendTemplateParams(id)
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Collection not found"},
    })
    if itemId != nil {
        req.QueryParam("item_id", itemId)
    }
    
    httpCtx, err := req.Call()
    if err != nil {
        return httpCtx.Response, err
    }
    return httpCtx.Response, err
}

// GetVideoCollectionItems takes context, id, page, perPage, shareCode, sort as parameters and
// returns an models.ApiResponse with models.CollectionItemDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists the IDs of videos in a collection and the date that each was added.
func (v *VideosController) GetVideoCollectionItems(
    ctx context.Context,
    id string,
    page *int,
    perPage *int,
    shareCode *string,
    sort *models.Sort1Enum) (
    models.ApiResponse[models.CollectionItemDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/collections/%v/items")
    req.AppendTemplateParams(id)
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Collection not found"},
    })
    if page != nil {
        req.QueryParam("page", *page)
    }
    if perPage != nil {
        req.QueryParam("per_page", *perPage)
    }
    if shareCode != nil {
        req.QueryParam("share_code", *shareCode)
    }
    if sort != nil {
        req.QueryParam("sort", *sort)
    }
    
    var result models.CollectionItemDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.CollectionItemDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// AddVideoCollectionItems takes context, id, body as parameters and
// returns an *Response and
// an error if there was an issue with the request or response.
// This endpoint adds one or more videos to a collection by video IDs.
func (v *VideosController) AddVideoCollectionItems(
    ctx context.Context,
    id string,
    body models.CollectionItemRequest) (
    *http.Response,
    error) {
    req := v.prepareRequest(ctx, "POST", "/v2/videos/collections/%v/items")
    req.AppendTemplateParams(id)
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Collection not found"},
    })
    req.Header("Content-Type", "application/json")
    req.Json(&body)
    
    httpCtx, err := req.Call()
    if err != nil {
        return httpCtx.Response, err
    }
    return httpCtx.Response, err
}

// GetVideoLicenseList takes context, videoId, license, page, perPage, sort, username, startDate, endDate, downloadAvailability, teamHistory as parameters and
// returns an models.ApiResponse with models.DownloadHistoryDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists existing licenses.
func (v *VideosController) GetVideoLicenseList(
    ctx context.Context,
    videoId *string,
    license *string,
    page *int,
    perPage *int,
    sort *models.Sort1Enum,
    username *string,
    startDate *time.Time,
    endDate *time.Time,
    downloadAvailability *models.DownloadAvailabilityEnum,
    teamHistory *bool) (
    models.ApiResponse[models.DownloadHistoryDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/licenses")
    
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    if videoId != nil {
        req.QueryParam("video_id", *videoId)
    }
    if license != nil {
        req.QueryParam("license", *license)
    }
    if page != nil {
        req.QueryParam("page", *page)
    }
    if perPage != nil {
        req.QueryParam("per_page", *perPage)
    }
    if sort != nil {
        req.QueryParam("sort", *sort)
    }
    if username != nil {
        req.QueryParam("username", *username)
    }
    if startDate != nil {
        req.QueryParam("start_date", startDate.Format(time.RFC3339))
    }
    if endDate != nil {
        req.QueryParam("end_date", endDate.Format(time.RFC3339))
    }
    if downloadAvailability != nil {
        req.QueryParam("download_availability", *downloadAvailability)
    }
    if teamHistory != nil {
        req.QueryParam("team_history", *teamHistory)
    }
    var result models.DownloadHistoryDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.DownloadHistoryDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// LicenseVideos takes context, body, subscriptionId, size, searchId as parameters and
// returns an models.ApiResponse with models.LicenseVideoResultDataList data and
// an error if there was an issue with the request or response.
// This endpoint gets licenses for one or more videos. You must specify the video IDs in the body parameter and the size and subscription ID either in the query parameter or with each video ID in the body parameter. Values in the body parameter override values in the query parameters. The download links in the response are valid for 8 hours.
func (v *VideosController) LicenseVideos(
    ctx context.Context,
    body models.LicenseVideoRequest,
    subscriptionId *string,
    size *models.Size9Enum,
    searchId *string) (
    models.ApiResponse[models.LicenseVideoResultDataList],
    error) {
    req := v.prepareRequest(ctx, "POST", "/v2/videos/licenses")
    
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    req.Header("Content-Type", "application/json")
    if subscriptionId != nil {
        req.QueryParam("subscription_id", *subscriptionId)
    }
    if size != nil {
        req.QueryParam("size", *size)
    }
    if searchId != nil {
        req.QueryParam("search_id", *searchId)
    }
    req.Json(&body)
    var result models.LicenseVideoResultDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.LicenseVideoResultDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// DownloadVideos takes context, id, body as parameters and
// returns an models.ApiResponse with models.Url data and
// an error if there was an issue with the request or response.
// This endpoint redownloads videos that you have already received a license for.
func (v *VideosController) DownloadVideos(
    ctx context.Context,
    id string,
    body models.RedownloadVideo) (
    models.ApiResponse[models.Url],
    error) {
    req := v.prepareRequest(ctx, "POST", "/v2/videos/licenses/%v/downloads")
    req.AppendTemplateParams(id)
    req.Authenticate(NewAuth("customer_accessCode"))
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    req.Header("Content-Type", "application/json")
    req.Json(&body)
    
    var result models.Url
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.Url](decoder)
    return models.NewApiResponse(result, resp), err
}

// SearchVideos takes context, addedDate, addedDateStart, addedDateEnd, aspectRatio, category, contributor, contributorCountry, duration, durationFrom, durationTo, fps, fpsFrom, fpsTo, keywordSafeSearch, language, license, model, page, perPage, peopleAge, peopleEthnicity, peopleGender, peopleNumber, peopleModelReleased, query, resolution, safe, sort, view as parameters and
// returns an models.ApiResponse with models.VideoSearchResults data and
// an error if there was an issue with the request or response.
// This endpoint searches for videos. If you specify more than one search parameter, the API uses an AND condition. Array parameters can be specified multiple times; in this case, the API uses an AND or an OR condition with those values, depending on the parameter. You can also filter search terms out in the `query` parameter by prefixing the term with NOT.
func (v *VideosController) SearchVideos(
    ctx context.Context,
    addedDate *time.Time,
    addedDateStart *time.Time,
    addedDateEnd *time.Time,
    aspectRatio *models.AspectRatioEnum,
    category *string,
    contributor []string,
    contributorCountry []string,
    duration *int,
    durationFrom *int,
    durationTo *int,
    fps *float64,
    fpsFrom *float64,
    fpsTo *float64,
    keywordSafeSearch *bool,
    language *models.LanguageEnum,
    license []models.License5Enum,
    model []string,
    page *int,
    perPage *int,
    peopleAge *models.PeopleAge1Enum,
    peopleEthnicity []models.PeopleEthnicity3Enum,
    peopleGender *models.PeopleGender1Enum,
    peopleNumber *int,
    peopleModelReleased *bool,
    query *string,
    resolution *models.ResolutionEnum,
    safe *bool,
    sort *models.Sort4Enum,
    view *models.View1Enum) (
    models.ApiResponse[models.VideoSearchResults],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/search")
    
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Not found"},
    })
    if addedDate != nil {
        req.QueryParam("added_date", addedDate.Format(models.DEFAULT_DATE))
    }
    if addedDateStart != nil {
        req.QueryParam("added_date_start", addedDateStart.Format(models.DEFAULT_DATE))
    }
    if addedDateEnd != nil {
        req.QueryParam("added_date_end", addedDateEnd.Format(models.DEFAULT_DATE))
    }
    if aspectRatio != nil {
        req.QueryParam("aspect_ratio", *aspectRatio)
    }
    if category != nil {
        req.QueryParam("category", *category)
    }
    if contributor != nil {
        req.QueryParam("contributor", contributor)
    }
    if contributorCountry != nil {
        req.QueryParam("contributor_country", contributorCountry)
    }
    if duration != nil {
        req.QueryParam("duration", *duration)
    }
    if durationFrom != nil {
        req.QueryParam("duration_from", *durationFrom)
    }
    if durationTo != nil {
        req.QueryParam("duration_to", *durationTo)
    }
    if fps != nil {
        req.QueryParam("fps", *fps)
    }
    if fpsFrom != nil {
        req.QueryParam("fps_from", *fpsFrom)
    }
    if fpsTo != nil {
        req.QueryParam("fps_to", *fpsTo)
    }
    if keywordSafeSearch != nil {
        req.QueryParam("keyword_safe_search", *keywordSafeSearch)
    }
    if language != nil {
        req.QueryParam("language", *language)
    }
    if license != nil {
        req.QueryParam("license", license)
    }
    if model != nil {
        req.QueryParam("model", model)
    }
    if page != nil {
        req.QueryParam("page", *page)
    }
    if perPage != nil {
        req.QueryParam("per_page", *perPage)
    }
    if peopleAge != nil {
        req.QueryParam("people_age", *peopleAge)
    }
    if peopleEthnicity != nil {
        req.QueryParam("people_ethnicity", peopleEthnicity)
    }
    if peopleGender != nil {
        req.QueryParam("people_gender", *peopleGender)
    }
    if peopleNumber != nil {
        req.QueryParam("people_number", *peopleNumber)
    }
    if peopleModelReleased != nil {
        req.QueryParam("people_model_released", *peopleModelReleased)
    }
    if query != nil {
        req.QueryParam("query", *query)
    }
    if resolution != nil {
        req.QueryParam("resolution", *resolution)
    }
    if safe != nil {
        req.QueryParam("safe", *safe)
    }
    if sort != nil {
        req.QueryParam("sort", *sort)
    }
    if view != nil {
        req.QueryParam("view", *view)
    }
    var result models.VideoSearchResults
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.VideoSearchResults](decoder)
    return models.NewApiResponse(result, resp), err
}

// GetVideoSuggestions takes context, query, limit as parameters and
// returns an models.ApiResponse with models.Suggestions data and
// an error if there was an issue with the request or response.
// This endpoint provides autocomplete suggestions for partial search terms.
func (v *VideosController) GetVideoSuggestions(
    ctx context.Context,
    query string,
    limit *int) (
    models.ApiResponse[models.Suggestions],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/search/suggestions")
    
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    req.QueryParam("query", query)
    if limit != nil {
        req.QueryParam("limit", *limit)
    }
    var result models.Suggestions
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.Suggestions](decoder)
    return models.NewApiResponse(result, resp), err
}

// GetUpdatedVideos takes context, startDate, endDate, interval, page, perPage, sort as parameters and
// returns an models.ApiResponse with models.UpdatedMediaDataList data and
// an error if there was an issue with the request or response.
// This endpoint lists videos that have been updated in the specified time period to update content management systems (CMS) or digital asset management (DAM) systems. In most cases, use the `interval` parameter to show videos that were updated recently, but you can also use the `start_date` and `end_date` parameters to specify a range of no more than three days. Do not use the `interval` parameter with either `start_date` or `end_date`.
func (v *VideosController) GetUpdatedVideos(
    ctx context.Context,
    startDate *time.Time,
    endDate *time.Time,
    interval *string,
    page *int,
    perPage *int,
    sort *models.Sort1Enum) (
    models.ApiResponse[models.UpdatedMediaDataList],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/updated")
    
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    if startDate != nil {
        req.QueryParam("start_date", startDate.Format(models.DEFAULT_DATE))
    }
    if endDate != nil {
        req.QueryParam("end_date", endDate.Format(models.DEFAULT_DATE))
    }
    if interval != nil {
        req.QueryParam("interval", *interval)
    }
    if page != nil {
        req.QueryParam("page", *page)
    }
    if perPage != nil {
        req.QueryParam("per_page", *perPage)
    }
    if sort != nil {
        req.QueryParam("sort", *sort)
    }
    var result models.UpdatedMediaDataList
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.UpdatedMediaDataList](decoder)
    return models.NewApiResponse(result, resp), err
}

// GetVideo takes context, id, language, view, searchId as parameters and
// returns an models.ApiResponse with models.Video data and
// an error if there was an issue with the request or response.
// This endpoint shows information about a video, including URLs to previews and the sizes that it is available in.
func (v *VideosController) GetVideo(
    ctx context.Context,
    id string,
    language *models.LanguageEnum,
    view *models.View1Enum,
    searchId *string) (
    models.ApiResponse[models.Video],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/%v")
    req.AppendTemplateParams(id)
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
        "404": {Message: "Not found"},
    })
    if language != nil {
        req.QueryParam("language", *language)
    }
    if view != nil {
        req.QueryParam("view", *view)
    }
    if searchId != nil {
        req.QueryParam("search_id", *searchId)
    }
    
    var result models.Video
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.Video](decoder)
    return models.NewApiResponse(result, resp), err
}

// FindSimilarVideos takes context, id, language, page, perPage, view as parameters and
// returns an models.ApiResponse with models.VideoSearchResults data and
// an error if there was an issue with the request or response.
// This endpoint searches for videos that are similar to a video that you specify.
func (v *VideosController) FindSimilarVideos(
    ctx context.Context,
    id string,
    language *models.LanguageEnum,
    page *int,
    perPage *int,
    view *models.View1Enum) (
    models.ApiResponse[models.VideoSearchResults],
    error) {
    req := v.prepareRequest(ctx, "GET", "/v2/videos/%v/similar")
    req.AppendTemplateParams(id)
    req.Authenticate(
        NewOrAuth(
            NewAuth("basic"),
            NewAuth("customer_accessCode"),
        ),
    )
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    if language != nil {
        req.QueryParam("language", *language)
    }
    if page != nil {
        req.QueryParam("page", *page)
    }
    if perPage != nil {
        req.QueryParam("per_page", *perPage)
    }
    if view != nil {
        req.QueryParam("view", *view)
    }
    
    var result models.VideoSearchResults
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.VideoSearchResults](decoder)
    return models.NewApiResponse(result, resp), err
}
