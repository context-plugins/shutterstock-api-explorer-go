// Package shutterstockapiexplorer
// This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
package shutterstockapiexplorer

import (
    "context"
    "github.com/apimatic/go-core-runtime/https"
    "github.com/apimatic/go-core-runtime/utilities"
    "net/http"
    "shutterstockapiexplorer/models"
)

// OauthController represents a controller struct.
type OauthController struct {
    baseController
}

// NewOauthController creates a new instance of OauthController.
// It takes a baseController as a parameter and returns a pointer to the OauthController.
func NewOauthController(baseController baseController) *OauthController {
    oauthController := OauthController{baseController: baseController}
    return &oauthController
}

// CreateAccessToken takes context, clientId, grantType, clientSecret, code, expires, realm, refreshToken as parameters and
// returns an models.ApiResponse with models.OauthAccessTokenResponse data and
// an error if there was an issue with the request or response.
// This endpoint returns an access token for the specified user and with the specified scopes. The token does not expire until the user changes their password. The body parameters must be encoded as form data.
func (o *OauthController) CreateAccessToken(
    ctx context.Context,
    clientId string,
    grantType models.GrantTypeEnum,
    clientSecret *string,
    code *string,
    expires *models.ExpiresEnum,
    realm *models.Realm1Enum,
    refreshToken *string) (
    models.ApiResponse[models.OauthAccessTokenResponse],
    error) {
    req := o.prepareRequest(ctx, "POST", "/v2/oauth/access_token")
    
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    req.Header("Content-Type", "application/x-www-form-urlencoded")
    req.FormParam("client_id", clientId)
    req.FormParam("grant_type", grantType)
    if clientSecret != nil {
        req.FormParam("client_secret", *clientSecret)
    }
    if code != nil {
        req.FormParam("code", *code)
    }
    if expires != nil {
        req.FormParam("expires", *expires)
    }
    if realm != nil {
        req.FormParam("realm", *realm)
    }
    if refreshToken != nil {
        req.FormParam("refresh_token", *refreshToken)
    }
    var result models.OauthAccessTokenResponse
    decoder, resp, err := req.CallAsJson()
    if err != nil {
        return models.NewApiResponse(result, resp), err
    }
    
    result, err = utilities.DecodeResults[models.OauthAccessTokenResponse](decoder)
    return models.NewApiResponse(result, resp), err
}

// Authorize takes context, clientId, redirectUri, responseType, state, realm, scope as parameters and
// returns an *Response and
// an error if there was an issue with the request or response.
// This endpoint returns a redirect URI (in the 'Location' header) that the customer uses to authorize your application and, together with POST /v2/oauth/access_token, generate an access token that represents that authorization.
func (o *OauthController) Authorize(
    ctx context.Context,
    clientId string,
    redirectUri string,
    responseType models.ResponseTypeEnum,
    state string,
    realm *models.Realm2Enum,
    scope *string) (
    *http.Response,
    error) {
    req := o.prepareRequest(ctx, "GET", "/v2/oauth/authorize")
    
    req.AppendErrors(map[string]https.ErrorBuilder[error]{
        "400": {Message: "Bad Request"},
        "401": {Message: "Unauthorized"},
        "403": {Message: "Forbidden"},
    })
    req.QueryParam("client_id", clientId)
    req.QueryParam("redirect_uri", redirectUri)
    req.QueryParam("response_type", responseType)
    req.QueryParam("state", state)
    if realm != nil {
        req.QueryParam("realm", *realm)
    }
    if scope != nil {
        req.QueryParam("scope", *scope)
    }
    httpCtx, err := req.Call()
    if err != nil {
        return httpCtx.Response, err
    }
    return httpCtx.Response, err
}
